<% if @event.errors.any? %>
  $('#ajax_body').html("<%= j (render 'events/form', show_form_buttons: true) %>");
<% else %>
  <%= render 'month_events', event: @event, event_instances: @event_instances %>
  $('#myModal').modal('hide');
<% end %>